{
  "name": "cypress_test1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Use npm run cy:run or cy:open\" && exit 0",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:chrome": "cypress run --browser chrome",
    "cy:run:headless": "cypress run --headless",
    "cy:ui": "cypress run --spec 'cypress/e2e/ui/**'",
    "cy:api": "cypress run --spec 'cypress/e2e/api/**'",
    "cy:hybrid": "cypress run --spec 'cypress/e2e/hybrid/**'",
    "generate:excel": "node scripts/generate_excel.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^15.10.0",
    "xlsx": "^0.18.5"
  }
}
